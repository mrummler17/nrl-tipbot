# NRL TipBot currently requires no environment variables to build or run.
#
# Optional local tooling:
# GITHUB_TOKEN=replace-with-a-token-only-if-gh-cli-cannot-authenticate
# NRL_TIPBOT_BACKUP_DIR=./backups
#
# Never commit real tokens, cookies, betting account credentials, or API keys.

